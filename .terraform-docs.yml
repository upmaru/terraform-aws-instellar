formatter: "markdown" # this is required

recursive:
  enabled: true
  path: modules

output:
  file: "README.md"
  mode: inject
  template: |
    <!-- BEGIN_TF_DOCS -->
    {{ .Content }}
    <!-- END_TF_DOCS -->